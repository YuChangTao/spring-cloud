server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #配置Nacos地址
        server-addr: 172.31.140.215:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'
